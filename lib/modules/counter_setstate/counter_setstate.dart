import 'package:flutter/material.dart';

class CounterSetStatePage extends StatelessWidget {
  static const String id = "/counter-setstate";
  const CounterSetStatePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
